# 任务启动卡：《巷口花食铺》MVP

更新时间：2026-09-10

## 当前目标

继续把项目从“可玩的 MVP”推进到“可在微信开发者工具与真机稳定验收的技术体验版”。当前生产方向是 `wechat-client/` 的 Cocos Creator 3.8.8 客户端；Web 版本保留为快速验证和参考实现。

本阶段优先级：

1. 保证 Web/Cocos 核心领域规则不继续静默分叉。
2. 所有 PR 通过 GitHub Actions 的 `npm run verify`。
3. 使用测试/正式 AppID 跑通微信开发者工具首局闭环。
4. 验证真实云存档、前后台切换与存档冲突路径。
5. 验证真实激励视频：完整观看、跳过、加载失败、重复请求。
6. 补齐正式美术和 Day 1 情绪反馈，但不在平台链路未稳定前扩张大系统。

## 当前已完成

- Web MVP 核心循环：生成 -> 合成 -> 制作 -> 订单 -> 奖励 -> 店铺升级。
- 本地存档、教程、今日任务、图谱、缺口/来源提示。
- Cocos Creator 3.8.8 工程、Boot 场景和竖屏布局。
- Cocos 领域层、应用层、平台适配层与 UI。
- 微信 `wx.login`、本地存档、云存档接口、激励视频、分享、生命周期和埋点适配。
- 云函数 OpenID 身份、服务器时间、存档读写、revision 冲突控制和 analytics。
- Web/Cocos 自动试玩、移动端 Playwright、Cocos UI/领域/云函数检查。
- GitHub Actions 通用验证门禁。

## 当前技术约束

- Web 与 Cocos 仍保留两份领域实现。
- `types.ts`、`dayTasks.ts` 必须严格一致。
- `catalog.ts`、`state.ts` 允许 Cocos 有生产扩展，但不得丢失 Web 现有公共能力。
- 修改领域代码后必须通过 `npm run domain:parity`。
- 中期应抽取单一共享 `game-core`，不要长期依赖复制同步。

## 本阶段明确不优先

- 支付/内购。
- 好友互访、排行榜、玩家 UGC。
- 自由摆放式复杂装修。
- 大规模长期运营系统。
- 在真实微信链路未稳定前继续无限扩充订单与玩法。

## 验收标准

### 通用 PR 门禁

```bash
npm run verify
```

必须通过，可在 GitHub Actions/Linux 环境执行，不依赖本机 GUI 工具。

### 本机完整门禁

```bash
npm run verify:local
```

用于装有 Cocos Creator 3.8.8 的开发机，额外覆盖 Cocos Web/微信构建、Cocos Playwright 和工具链检查。

### 微信技术体验版门禁

- 测试/正式 AppID 可导入开发者工具。
- 首屏非空且触摸可用。
- 教程、首单、故事单、制作、升级完整可走通。
- 前后台切换后状态与倒计时正确。
- 云存档首次创建、覆盖、冲突和恢复路径可验证。
- 激励广告完整观看才发奖励；跳过/失败不误发。
- 至少一轮真机小屏与主流机型兼容验证通过。

## 开发原则

- 新功能优先配置化。
- 平台能力不得渗透到领域层。
- 不复制竞品名称、角色、美术、UI、文案或数值表。
- 每个新增系统都必须说明服务核心循环的哪个节点。
- 文档必须跟随代码阶段更新，避免 Codex/AI 使用过期上下文。
